import { Sx } from '@mantine/core';
import { CSSProperties } from 'react';
import { DataTableColumn } from './DataTable.props';
declare type DataTableRowCellProps<T> = {
    className?: string;
    sx?: Sx;
    style?: CSSProperties;
    record: T;
} & Pick<DataTableColumn<T>, 'accessor' | 'visibleMediaQuery' | 'textAlignment' | 'width' | 'ellipsis' | 'render'>;
export default function DataTableRowCell<T>({ className, sx, style, visibleMediaQuery, record, ellipsis, textAlignment, width, accessor, render, }: DataTableRowCellProps<T>): JSX.Element | null;
export {};
//# sourceMappingURL=DataTableRowCell.d.ts.map