import { MantineColor } from '@mantine/core';
import { ReactNode } from 'react';
declare type DataTableRowMenuItemProps = {
    icon?: ReactNode;
    title: ReactNode;
    color?: MantineColor;
    disabled?: boolean;
    onClick: () => void;
};
export default function DataTableRowMenuItem({ icon, title, color, disabled, onClick }: DataTableRowMenuItemProps): JSX.Element;
export {};
//# sourceMappingURL=DataTableRowMenuItem.d.ts.map