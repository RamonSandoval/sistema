import { MantineNumberSize, MantineShadow } from '@mantine/core';
import { ReactNode } from 'react';
declare type DataTableRowMenuProps = {
    borderRadius: MantineNumberSize | undefined;
    shadow: MantineShadow | undefined;
    zIndex: number | undefined;
    top: number;
    left: number;
    children: ReactNode;
    onDestroy: () => void;
};
export default function DataTableRowMenu({ borderRadius, shadow, zIndex, top: desiredTop, left: desiredLeft, onDestroy, children, }: DataTableRowMenuProps): JSX.Element;
export {};
//# sourceMappingURL=DataTableRowMenu.d.ts.map